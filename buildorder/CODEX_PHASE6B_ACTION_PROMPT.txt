Read `PHASE6B_REAL_PEER_XBRL_ACTIVATION_SPEC.md` completely and execute it end-to-end.

The goal is to fully activate real source-backed XBRL data for Shufersal and Rami Levy using the existing generic MAYA/XBRL architecture. Do not create issuer-specific parsers; use mapping profiles only where actual filings require issuer-specific aliases.

For each company:
- discover real MAYA reports dynamically
- resolve XBRL
- parse/map deterministically
- preserve annual / quarter-only / YTD semantics
- handle retailer IFRS 16 safely
- validate before activation
- persist to remote D1
- verify API
- switch frontend to generic API-backed loading
- disable silent mock fallback after activation

Do not add any LLM functionality.
Do not fabricate missing values.
Keep valuation incomplete while real market data is unavailable.

Also clean up stale contradictory current-state text in `DATA_MODEL.md`.

When finished:
- run all tests
- run worker tests/check
- run frontend build
- verify remote D1
- verify live API
- redeploy the existing `israel-stocks-api` Worker if needed
- verify Pages if frontend changed
- update README, DATA_MODEL.md, and CHATGPT_HANDOFF.md
- commit
- push to `origin/main`

Do not finish until both Shufersal and Rami Levy are genuinely source-backed, or a concrete issuer-specific blocker is documented with exact evidence and no invalid data is activated.
