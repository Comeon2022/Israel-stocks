All build specification files are located in:

C:\Users\Liorkale\Desktop\Israel-stocks\Israel-stocks\buildorder

Read and execute:

C:\Users\Liorkale\Desktop\Israel-stocks\Israel-stocks\buildorder\PHASE6F_WINDOWS_PERSISTENCE_AND_PEER_ACTIVATION_SPEC.md

Execute it end-to-end.

The current blocker is specific:
the TypeScript persistence adapter uses `npx`, which fails on Windows with `spawnSync npx ENOENT`.

Fix the command resolver so Windows uses `npx.cmd` and Unix-like systems use `npx`. Keep subprocess execution safe, keep `--remote`, and do not loosen validation.

Then:
- rerun Shufersal dry-run and real ingestion
- rerun Rami Levy dry-run and real ingestion
- verify remote D1
- verify idempotency with a second real run
- verify live API
- switch successfully activated peers to generic API-backed frontend loading
- run all tests/build/checks
- update README.md, DATA_MODEL.md, CHATGPT_HANDOFF.md
- deploy the existing Worker only if needed
- verify Pages if frontend changed
- commit
- push to origin/main

Do not create a new Worker or D1 database.
Do not add LLM functionality.
Do not fabricate data.
Do not mark PROCESSED until persistence succeeds.
