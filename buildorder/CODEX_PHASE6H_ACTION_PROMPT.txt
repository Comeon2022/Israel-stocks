All build specification files are located in:

C:\Users\Liorkale\Desktop\Israel-stocks\Israel-stocks\buildorder

Read and execute:

C:\Users\Liorkale\Desktop\Israel-stocks\Israel-stocks\buildorder\PHASE6H_REMOVE_HARDCODED_NPX_AND_ACTIVATE_PEERS_SPEC.md

Execute it end-to-end.

The resolver already exists and is tested. The remaining bug is that the real persistence adapter still directly calls `execFileSync('npx', ...)` or equivalent.

Actually replace that active adapter executable with `resolveNpxCommand()` and prove the real adapter consumes it. Do not stop after only testing the resolver.

Then:
- rerun real Shufersal ingestion
- rerun real Rami Levy ingestion
- verify remote D1
- verify idempotency
- verify live API
- activate frontend API mode for successful peers
- run all tests/build/checks
- update README.md, DATA_MODEL.md, CHATGPT_HANDOFF.md
- deploy existing Worker only if needed
- verify Pages if frontend changed
- commit
- push to origin/main

Do not create a new Worker or database.
Do not add LLM functionality.
Do not fabricate data.
Do not mark PROCESSED before successful persistence.
