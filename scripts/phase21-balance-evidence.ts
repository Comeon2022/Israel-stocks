import { mkdirSync, writeFileSync } from 'node:fs'
const targets = [['castro','1728277'],['isrotel','1731504'],['fox','1729790'],['delta-israel-brands','1722944'],['dan-hotels','1732438']] as const
const fields = ['cash','shortTermDebt','longTermDebt'] as const
const reason = 'No cached exact XBRL fact or unambiguous consolidated FY2025 statement row passed the scope, unit, semantic, and non-lease proof gate; no activation permitted.'
mkdirSync('tmp/phase21',{recursive:true})
const packs=targets.map(([companyId,reportId])=>{const evidence=Object.fromEntries(fields.map(field=>[field,{status:'REJECTED_AMBIGUOUS',value:null,confidence:'LOW',sourceType:null,reportId,sourceFiles:{pdf:`tmp/phase17f24/${reportId}/source.pdf`,lines:`tmp/phase17f24/${reportId}/lines.json`},reason}]));const p={companyId,reportId:Number(reportId),fields:evidence,accepted:[],rejected:fields.map(field=>({field,candidateValue:null,reason})),leaseExclusion:'Lease liabilities excluded; no debt field was derived or lease-mixed.',writeEligible:[],remainingAmbiguity:reason};mkdirSync(`tmp/phase21/${companyId}`,{recursive:true});writeFileSync(`tmp/phase21/${companyId}/evidence.json`,JSON.stringify(p,null,2)+'\n');return p})
writeFileSync('tmp/phase21/master-evidence.json',JSON.stringify(packs,null,2)+'\n');writeFileSync('tmp/phase21/activation-plan.json',JSON.stringify({phase:'21',zeroWriteEvidencePass:true,eligible:[],reason:'No HIGH-confidence field passed the authoritative proof gate.'},null,2)+'\n');console.log(JSON.stringify({companies:packs.length,fields:packs.length*3,accepted:0,writes:0}))
