const route=process.argv[2]??'company/sano';const list=await (await fetch('http://127.0.0.1:9222/json')).json();const page=list.find(x=>x.type==='page');const ws=new WebSocket(page.webSocketDebuggerUrl);let id=0;const p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const send=(method,params={})=>new Promise(r=>{const i=++id;p.set(i,r);ws.send(JSON.stringify({id:i,method,params}))});await send('Page.navigate',{url:`https://israel-stocks.pages.dev/${route}`});await new Promise(r=>setTimeout(r,5000));const x=await send('Runtime.evaluate',{expression:`JSON.stringify((()=>{const text=document.body.innerText;const lines=text.split('\\n').map(s=>s.trim()).filter(Boolean);const key=/MOCK|GLOBES|TTM|IFRS|תמחור|נתוני|מחיר|שווי|מכפיל|2023|2024|2025|2026|רבעון|שנתי|אין נתוני שוק/i;return {route,lines:lines.filter(x=>key.test(x)).slice(0,120),yearCounts:Object.fromEntries([2023,2024,2025,2026].map(y=>[y,(text.match(new RegExp(String(y),'g'))||[]).length])),links:[...document.querySelectorAll('a')].map(a=>({text:a.innerText.trim(),href:a.href})).filter(x=>x.text)}})())`,returnByValue:true});console.log(x.result?.result?.value??'{}');ws.close()
