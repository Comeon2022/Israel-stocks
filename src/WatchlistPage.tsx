import {useEffect,useState} from 'react'
import {Link} from 'react-router-dom'
import {apiGet} from './api/client'
import {valuationCoverage} from './lib/valuationCoverage'
import {readWatchlist,toggleWatchlist,writeWatchlist} from './lib/watchlist'
const ids=['sano','shufersal','rami-levy','yochananof','neto-malinda','strauss','victory','tiv-taam','fox','max-stock','delta-israel-brands','castro','diplomat','isrotel','dan-hotels'];const show=(v:any)=>v==null?'UNAVAILABLE':typeof v==='number'?v.toLocaleString('en-US',{maximumFractionDigits:2}):String(v);const load=(id:string)=>Promise.all([apiGet<any>(`/api/companies/${id}/market/latest`),apiGet<any>(`/api/companies/${id}/fair-value`),apiGet<any>(`/api/companies/${id}/scorecard-v2`)]).then(([m,f,s])=>({market:m.market,fv:f,score:s})).catch(()=>({error:true}))
export function WatchlistPage(){const [list,setList]=useState<string[]>([]),[data,setData]=useState<Record<string,any>>({}),[selected,setSelected]=useState<string[]>([]),[loaded,setLoaded]=useState(false);useEffect(()=>{setList(readWatchlist(localStorage,ids));setLoaded(true)},[]);useEffect(()=>{if(list.length)Promise.all(list.map(id=>load(id).then(x=>[id,x] as const))).then(x=>setData(Object.fromEntries(x)))},[list.join(',')]);const toggle=(id:string)=>{const next=toggleWatchlist(localStorage,list,id,ids);setList(next);setSelected(s=>s.filter(x=>next.includes(x)))};const clear=()=>{if(confirm('Clear the local watchlist?')){writeWatchlist(localStorage,[],ids);setList([]);setSelected([])}};if(!loaded)return <div className="panel api-state"><h1>Loading watchlist...</h1></div>;if(!list.length)return <div className="panel empty"><h1>Watchlist</h1><p>Save companies locally to reopen and compare them later.</p><Link className="filter active" to="/companies">Browse companies</Link></div>;return <><div className="section-heading"><div><div className="eyebrow">INVESTOR WORKSPACE</div><h1>Watchlist</h1><p>{list.length} saved companies · {selected.length} / 4 selected</p></div><button className="filter" onClick={clear}>Clear watchlist</button></div>{selected.length>0&&<div className="filters">{selected.map(id=><button className="filter" key={id} onClick={()=>setSelected(s=>s.filter(x=>x!==id))}>{id} ×</button>)}{selected.length>=2&&<Link className="filter active" to={`/compare?companies=${selected.join(',')}`}>Compare selected ({selected.length})</Link>}<button className="filter" onClick={()=>setSelected([])}>Clear selection</button></div>}<div className="panel"><table><thead><tr><th>Company</th><th>Price</th><th>Market Cap</th><th>Coverage</th><th>Scorecard</th><th>FV1</th><th>FV2</th><th>Compare</th><th>Actions</th></tr></thead><tbody>{list.map(id=>{const d=data[id],cv=valuationCoverage(d?.fv?.methods);return <tr key={id}><td><Link className="text-link" to={`/company/${id}`}>{id}</Link></td><td dir="ltr">{show(d?.market?.currentPrice)}</td><td dir="ltr">{show(d?.market?.marketCap)}</td><td dir="ltr">{cv.availableMethods}/3 {cv.label}</td><td dir="ltr">{show(d?.score?.total)}</td><td dir="ltr">{d?.fv?.valuationScore?.available?show(d.fv.perShare?.base):'UNAVAILABLE'}</td><td dir="ltr">{d?.fv?.fv2?.available?show(d.fv.fv2.perShare?.base):'UNAVAILABLE'}</td><td><input type="checkbox" aria-label={`Select ${id} for comparison`} checked={selected.includes(id)} onChange={()=>setSelected(s=>s.includes(id)?s.filter(x=>x!==id):s.length<4?[...s,id]:s)}/></td><td><button className="filter" aria-label={`Remove ${id} from watchlist`} onClick={()=>toggle(id)}>Remove</button></td></tr>})}</tbody></table></div></>}
