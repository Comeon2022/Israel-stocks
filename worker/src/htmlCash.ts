export interface HtmlCashEvidence { reportId:string; attachmentType:'HTML'; field:'cash'; sourceConcept:string; rawLabel:string; scope:'CONSOLIDATED'|'SEPARATE'|'UNKNOWN'; fiscalYear:number|null; comparativeYear:number|null; rawCurrentValue:string|null; rawComparativeValue:string|null; unit:'ILS'|'THOUSANDS_ILS'|'MILLIONS_ILS'|null; normalizedCurrentILSm:number|null; normalizedComparativeILSm:number|null; contextEvidence:string[]; sourceLocation:string; confidence:'HIGH'|'LOW' }
const valueFor=(html:string,alias:string)=>{const re=new RegExp(`FieldAlias=["']${alias}["'][^>]*>([^<]+)<`,`gi`);return [...html.matchAll(re)].map(x=>x[1].trim()).filter(x=>/^\(?[-\d,\.]+\)?$/.test(x))}
export function extractHtmlCashEvidence(reportId:string,html:string):HtmlCashEvidence[]{const consolidated=valueFor(html,'CashEquivalentsConsolidated'),separate=valueFor(html,'CashEquivalentsSeparate');const out:HtmlCashEvidence[]=[];if(consolidated.length)out.push({reportId,attachmentType:'HTML',field:'cash',sourceConcept:'CashEquivalentsConsolidated',rawLabel:'CashEquivalentsConsolidated',scope:'CONSOLIDATED',fiscalYear:null,comparativeYear:null,rawCurrentValue:consolidated[0],rawComparativeValue:consolidated[1]??null,unit:null,normalizedCurrentILSm:null,normalizedComparativeILSm:null,contextEvidence:['structured alias present','year binding absent','unit binding absent'],sourceLocation:`FieldAlias=CashEquivalentsConsolidated occurrences=${consolidated.length}`,confidence:'LOW'});if(separate.length)out.push({reportId,attachmentType:'HTML',field:'cash',sourceConcept:'CashEquivalentsSeparate',rawLabel:'CashEquivalentsSeparate',scope:'SEPARATE',fiscalYear:null,comparativeYear:null,rawCurrentValue:separate[0],rawComparativeValue:separate[1]??null,unit:null,normalizedCurrentILSm:null,normalizedComparativeILSm:null,contextEvidence:['separate alias rejected'],sourceLocation:`FieldAlias=CashEquivalentsSeparate occurrences=${separate.length}`,confidence:'LOW'});return out}
