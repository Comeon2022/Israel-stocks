import {describe,expect,it} from 'vitest'
import {detectFragmentedHeadingBlocks} from './pdfTable'
const line=(text:string,y:number):any=>({pageNumber:1,text,y,items:[{pageNumber:1,text,x:180,y,width:220,height:12}]})
describe('fragmented heading reconstruction',()=>{it('reconstructs a two-line consolidated balance-sheet title',()=>{const blocks=detectFragmentedHeadingBlocks([line('Consolidated Statements',700),line('of Financial Position',684)]);expect(blocks[0].combinedText).toContain('Consolidated Statements of Financial Position');expect(blocks[0].scope).toBe('CONSOLIDATED');expect(blocks[0].confidence).toBe('HIGH')});it('supports a scope phrase on a separate adjacent line',()=>{const blocks=detectFragmentedHeadingBlocks([line('Statements of Financial Position',700),line('Consolidated',684)]);expect(blocks[0].statementType).toBe('BALANCE_SHEET');expect(blocks[0].scope).toBe('CONSOLIDATED')});it('does not merge a distant or numeric table line',()=>{const blocks=detectFragmentedHeadingBlocks([line('Consolidated Statements',700),line('of Financial Position',684),line('2025 2024',640)]);expect(blocks[0].rawLines).toHaveLength(2)});it('does not turn an unrelated narrative into a title block',()=>{const blocks=detectFragmentedHeadingBlocks([line('The consolidated business performed well',700),line('during the year with improving margins',684)]);expect(blocks).toHaveLength(0)})})
