import { GLOBES_MAP, fetchGlobesSnapshot } from './market/globes-provider'
export interface Env { DB:D1Database; ALLOWED_ORIGINS?:string }
interface D1Database { prepare(sql:string):{bind(...v:unknown[]):{all<T>():Promise<{results:T[]}>;first<T>():Promise<T|null>;run():Promise<unknown>}} }
type Row=Record<string,any>
const reply=(body:unknown,status=200,origin='*')=>new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json; charset=utf-8','access-control-allow-origin':origin}})
const allowed=(r:Request,e:Env)=>e.ALLOWED_ORIGINS?.split(',').includes(r.headers.get('origin')??'')?r.headers.get('origin')!:'*'
const map=(r:Row)=>({...r,fiscalQuarter:r.fiscal_quarter,periodType:r.period_type,periodEnd:r.period_end,flowBasis:r.flow_basis,sourceIds:JSON.parse(r.source_ids_json??'[]'),financials:r})
const periodKey=(r:Row)=>`${r.period_end}|${r.period_type}|${r.flow_basis??''}`
function unique(rows:Row[]){const seen=new Set<string>();return rows.filter(r=>{const k=periodKey(r);if(seen.has(k))return false;seen.add(k);return true})}
async function financialRows(db:D1Database,id:string){const r=await db.prepare('SELECT p.*,s.* FROM financial_periods p LEFT JOIN financial_statements s ON s.period_id=p.id WHERE p.company_id=? ORDER BY p.period_end,p.period_type,p.id').bind(id).all<Row>();return unique(r.results)}
function field(value:number|null,reason:string){return value==null?{value:null,status:'UNAVAILABLE',reason}:{value,status:'AVAILABLE',reason:null}}
function ttm(rows:Row[],companyId:string){const annual=rows.find(r=>r.period_type==='ANNUAL'&&r.fiscal_year===2025);const current=rows.find(r=>r.fiscal_year===2026&&(r.flow_basis==='YTD'||r.flow_basis==='ANNUAL'));const prior=rows.find(r=>r.fiscal_year===2025&&r.flow_basis==='YTD');const compatible=!!annual&&!!current&&!!prior;const fields:Record<string,any>={};for(const key of ['revenue','operating_income','ebitda_reported','net_income','cash_flow_from_operations','capex']){const value=compatible&&[annual[key],current[key],prior[key]].every(v=>typeof v==='number')?annual[key]+current[key]-prior[key]:null;fields[key]=field(value,compatible?'MISSING_INPUT':'INCOMPATIBLE_PERIOD_BASIS')}const fcf=compatible&&fields.cash_flow_from_operations.value!=null&&fields.capex.value!=null?fields.cash_flow_from_operations.value-fields.capex.value:null;fields.fcf=field(fcf,compatible?'MISSING_INPUT':'INCOMPATIBLE_PERIOD_BASIS');fields.adjustedFcf=field(null,companyId==='neto-malinda'?'NOT_APPLICABLE':'MISSING_INPUT');return {companyId,available:Object.values(fields).some((x:any)=>x.status==='AVAILABLE'),method:compatible?'FY2025 + current YTD - prior comparable YTD':null,sourcePeriods:{fy2025:annual?.id??null,currentYtd:current?.id??null,priorComparableYtd:prior?.id??null},fields,reason:compatible?null:'No compatible FY2025/current-YTD/prior-YTD period set'}}
async function market(db:D1Database,id:string){const m=await db.prepare('SELECT * FROM market_snapshots WHERE company_id=? ORDER BY COALESCE(as_of,snapshot_date) DESC LIMIT 1').bind(id).first<Row>();if(!m)return null;const f=await db.prepare("SELECT s.net_income,s.operating_income,s.ebitda_reported,s.cash_and_cash_equivalents,s.short_term_debt,s.long_term_debt,p.period_end FROM financial_periods p JOIN financial_statements s ON s.period_id=p.id WHERE p.company_id=? AND p.period_type='ANNUAL' ORDER BY p.period_end DESC LIMIT 1").bind(id).first<Row>();const debt=f?.short_term_debt!=null&&f?.long_term_debt!=null?f.short_term_debt+f.long_term_debt:null;const nd=debt!=null&&f?.cash_and_cash_equivalents!=null?debt-f.cash_and_cash_equivalents:null;const ev=nd!=null?m.market_cap+nd:null;const pe=f&&f.net_income>0?m.market_cap/f.net_income:null;const evEbit=f&&f.operating_income>0&&ev!=null?ev/f.operating_income:null;const evEbitda=f&&f.ebitda_reported>0&&ev!=null?ev/f.ebitda_reported:null;return {...m,day_change:m.day_change==null?null:m.day_change/100,valuation:{basis:f?{earnings:'LATEST_ANNUAL',ebitda:'LATEST_ANNUAL',ebit:'LATEST_ANNUAL',fcf:'UNAVAILABLE',balanceSheet:f.period_end}:null,periodEnd:f?.period_end??null,enterpriseValueIlsMillions:ev,pe,evEbit,evEbitda,priceToFcf:null,fcfYield:null,netDebtToMarketCap:nd!=null?nd/m.market_cap:null,netCashToMarketCap:nd!=null&&nd<0?-nd/m.market_cap:null,unavailableReasons:nd==null?['MISSING_NET_DEBT_INPUTS']:[]}}}
async function refresh(db:D1Database){for(const id of Object.keys(GLOBES_MAP))try{const s=await fetchGlobesSnapshot(id);await db.prepare(`INSERT OR REPLACE INTO market_snapshots(id,company_id,provider,provider_instrument_id,security_id,exchange,currency,snapshot_date,as_of,fetched_at,share_price,previous_close,day_change,day_change_pct,market_cap,shares_outstanding,source_id,data_status,validation_status,raw_price,raw_price_unit,delay_minutes,created_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,datetime('now'))`).bind(`globes-${id}-${s.asOf}`,id,s.provider,s.providerInstrumentId,s.securityId,s.exchange,s.currency,s.asOf,s.asOf,s.fetchedAt,s.priceIls,s.previousCloseIls,s.dayChange==null?null:s.dayChange/100,s.dayChangePct,s.marketCapIlsMillions,s.sharesOutstanding,s.sourceRef,'VERIFIED','PASS',s.rawPrice,s.rawPriceUnit,s.delayMinutes).run()}catch(e){console.error('Globes refresh failed',id,e)}}
export default {async fetch(req:Request,env:Env){const origin=allowed(req,env);const u=new URL(req.url);const p=u.pathname.split('/').filter(Boolean);try{if(p.length===2&&p[0]==='api'&&p[1]==='health')return reply({status:'ok',service:'israel-stocks-api'},200,origin);if(p[0]!=='api'||p[1]!=='companies')return reply({error:{code:'NOT_FOUND'}},404,origin);const id=p[2];if(!id)return reply({companies:(await env.DB.prepare('SELECT * FROM companies WHERE is_active=1').bind().all()).results},200,origin);if(p.length===3)return reply({company:await env.DB.prepare('SELECT * FROM companies WHERE id=? AND is_active=1').bind(id).first()},200,origin);if(p[3]==='market'&&p[4]==='latest')return reply({market:await market(env.DB,id)},200,origin);if(p[3]==='sources')return reply({sources:(await env.DB.prepare('SELECT * FROM financial_sources WHERE company_id=?').bind(id).all()).results},200,origin);if(p[3]==='validation')return reply({validation:(await env.DB.prepare('SELECT * FROM validation_results WHERE company_id=?').bind(id).all()).results},200,origin);if(p[3]==='financials'){const rows=await financialRows(env.DB,id);if(p[4]==='latest'){const r=rows.filter(x=>x.period_type==='ANNUAL').at(-1)??rows.at(-1);return reply({financial:r?map(r):null},200,origin)}if(p[4]==='ttm')return reply({ttm:ttm(rows,id)},200,origin);const filter=u.searchParams.get('periodType');if(filter&&filter!=='ANNUAL'&&filter!=='QUARTERLY')return reply({error:{code:'INVALID_PERIOD_TYPE',message:'periodType must be ANNUAL or QUARTERLY'}},400,origin);const filtered=filter==='ANNUAL'?rows.filter(r=>r.period_type==='ANNUAL'):filter==='QUARTERLY'?rows.filter(r=>r.period_type==='QUARTERLY'):rows;return reply({financials:filtered.map(map)},200,origin)}return reply({error:{code:'NOT_FOUND'}},404,origin)}catch(e){return reply({error:{code:'DATABASE_ERROR',message:e instanceof Error?e.message:'Unknown error'}},500,origin)}},async scheduled(e:ScheduledController,env:Env){if(e.cron!=='0 6 * * *')await refresh(env.DB)}}
interface ScheduledController {cron:string}
