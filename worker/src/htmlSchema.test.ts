import {describe,expect,it} from 'vitest'
import {inventoryHtmlSchema} from './htmlSchema'
describe('HTML schema inventory',()=>{it('captures exact field identifiers without inferring context',()=>{const x=inventoryHtmlSchema('r','<tr id="row7"><td><span id="Field747" FieldAlias="CashEquivalentsConsolidated">74,243</span></td></tr>')[0];expect(x.rawValue).toBe('74,243');expect(x.metadataIdentifiers).toContain('Field747');expect(x.yearEvidence).toEqual([]);expect(x.unitEvidence).toEqual([]);expect(x.blocker).toContain('NO_FIELD_ID')});it('records explicit same-row metadata for later validation',()=>{const x=inventoryHtmlSchema('r','<tr data-section-id="s1"><td><span FieldAlias="CashEquivalentsConsolidated" data-year="2025" data-unit="thousand ILS">10</span></td></tr>')[0];expect(x.rowIdentifiers['data-section-id']).toBe('s1');expect(x.yearEvidence).toEqual(['2025']);expect(x.unitEvidence).toContain('thousand')})})
