# Phase 17F.27 — Expanded-universe FY2025 balance recovery

This phase stopped before the guarded pre-write gate. The fresh remote D1 snapshot was blocked by Wrangler/Cloudflare authorization error 7403, before any query or mutation. No production field, provenance row, source, period, market snapshot, score, or valuation state was changed. No repair SQL was generated beyond an explicit zero-statement marker.

## Preflight

The prior Phase 17F.26 read-only snapshot showed all three target fields—cash, short-term debt, and long-term debt—NULL for every target. A fresh 17F.27 remote verification could not be completed because Wrangler returned 7403. The selected report identities remain the fixed MAYA IDs from the prior snapshot. Lease-liability columns were not included in debt.

## Source-proof decision

The available PDF token evidence contains cash-equivalent candidates, but several reports require careful balance-sheet page binding and debt aggregation across current maturities, debentures, and bank borrowings. Some extracted candidate lines are narrative or note contexts. Because this phase forbids residual inference, comparative substitution, broad cash definitions, and lease mixing, no candidate was accepted without a completed field-level consolidated scope/unit proof. No transition was executed and no value was fabricated.

The machine-readable preflight, source-evidence rejection list, transition matrix, and zero-statement SQL marker are under `tmp/phase17f27/`. A separately scoped PDF/XBRL evidence phase is required before production recovery.

No Scorecard/FV rules, classes, capex resolver, lease policy, or market data were changed. No Worker deployment occurred.
