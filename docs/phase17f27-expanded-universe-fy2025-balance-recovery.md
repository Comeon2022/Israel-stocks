# Phase 17F.27 — Expanded-universe FY2025 balance recovery

This phase stopped at the guarded pre-write gate. No production field, provenance row, source, period, market snapshot, score, or valuation state was changed. No repair SQL was generated beyond an explicit zero-statement marker.

## Preflight

The exact ten FY2025 periods and report IDs were checked. All three target fields—cash, short-term debt, and long-term debt—are currently NULL for every target. The selected report identities remain the fixed MAYA IDs. Lease-liability columns were captured separately and were not included in debt.

## Source-proof decision

The available PDF token evidence contains cash-equivalent rows, but several reports require careful balance-sheet page binding and debt aggregation across current maturities, debentures, and bank borrowings. Some extracted candidate lines are narrative or note contexts. Because this phase forbids residual inference, comparative substitution, broad cash definitions, and lease mixing, no candidate was accepted without a completed field-level consolidated scope/unit proof. Consequently, all 30 target transitions remain `NULL_TO_NULL`; no value was fabricated.

The machine-readable preflight, source-evidence rejection list, transition matrix, and zero-statement SQL marker are under `tmp/phase17f27/`. A separately scoped PDF/XBRL evidence phase is required before production recovery.

No Scorecard/FV rules, classes, capex resolver, lease policy, or market data were changed. No Worker deployment occurred.
